// Debug server mode.
// This mode is designed for rapid development. This includes using 'localhost:8980' as the api target instead of
// wherever this application is served from.

export const environment = {
  production: false,
  serve: true
};

import 'zone.js/dist/zone-error';
